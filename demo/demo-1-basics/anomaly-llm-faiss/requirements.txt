# -----------------------------
# FastAPI stack
# -----------------------------
fastapi==0.110.0
uvicorn==0.27.0
httpx==0.27.0
python-dotenv==1.0.1

# -----------------------------
# Infra / orchestration
# -----------------------------
redis==5.0.1
kubernetes==29.0.0
openshift==0.13.1
psutil==5.9.8

# -----------------------------
# LLM / embeddings (CPU ONLY)
# -----------------------------
sentence-transformers==2.5.1
transformers==4.37.2
faiss-cpu==1.7.4

# 🚫 Prevent CUDA pulls
torch==2.1.2+cpu ; platform_system != "Windows"
--extra-index-url https://download.pytorch.org/whl/cpu

# -----------------------------
# LangChain (compatible pins)
# -----------------------------
langchain==0.1.16
langchain-community==0.0.34

# -----------------------------
# Tokenization / LLM APIs
# -----------------------------
tiktoken==0.6.0
openai==1.12.0

# -----------------------------
# Debug / dev (CI-safe)
# -----------------------------
ipython==8.22.2
py-spy==0.3.14
